
  ╔══════════════════════════════════════════════════════════════════════════════════╗
  ║                    PE ORG-AI-R PLATFORM — ARCHITECTURE SUMMARY                   ║
  ╚══════════════════════════════════════════════════════════════════════════════════╝

  Private equity portfolio analysis platform (CS1-CS5) evaluating companies against
  a 7-dimension AI Readiness (V^R) framework to produce Org-AI-R scores for
  Investment Committee decisions.

  CS3 Scoring Portfolio:   NVDA | JPM | WMT | GE | DG  (5 companies)
  CS5 Dashboard Portfolio: NVDA | CRM | GOOGL | JPM | WMT | ADP | UNH  (7 companies)

  ━━━━━━━━━━━━━━━━━━━━━━━ PIPELINE OVERVIEW ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  CS1  Company Registration + Groq Metadata Enrichment
   ↓
  CS2  SEC Filing Collection → Parsing → Chunking → Signal Scoring
   ↓
  CS3  7-Dimension V^R Rubric Scoring → TC → H^R → Position Factor → Org-AI-R
   ↓
  CS4  ChromaDB RAG Indexing → Hybrid Retrieval → Chatbot → IC Prep Package
   ↓
  CS5  LangGraph Multi-Agent DD Workflow → HITL Approval → Value Creation → Reporting

  Layer flow:  Routers → Services → Pipelines / Repositories → Data Stores

  ━━━━━━━━━━━━━━━━━━━━━━━ CS1: COMPANY REGISTRATION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/companies  { ticker, name }                                   │
  │                                                                              │
  │  1. Create company record ──────────────────────────► Snowflake: companies   │
  │                                                                              │
  │  2. Background: Groq metadata enrichment                                     │
  │     └─ sector, revenue_estimate, employee_count ──► Snowflake: companies     │
  │                                                                              │
  │  3. Background: Create portfolio entry ────────────► Snowflake: portfolio    │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━━━━━ CS2: SIGNAL COLLECTION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    EXTERNAL SOURCES                   SIGNAL SERVICES                  OUTPUTS
    ─────────────────                  ───────────────                  ───────
    LinkedIn / Indeed                ┌─ job_signal_service.py ─────────► technology_hiring   [0-100]
    Glassdoor                        │
    PatentsView / USPTO ─────────────┤─ patent_signal_service.py ──────► innovation_activity [0-100]
                                     │
    BuiltWith / Wappalyzer ──────────┤─ tech_signal_service.py ────────► digital_presence    [0-100]
    Groq (subdomains)                │
                                     │
    SEC DEF-14A (proxy stmt) ────────┤─ leadership_service.py ─────────► leadership_signals  [0-100]
                                     │
    SEC proxy (board bios) ──────────┤─ board_composition_service.py ──► board_composition   [0-100]
                                     │
    Glassdoor reviews (S3) ──────────┘─ culture_signal_service.py ─────► culture             [0-100]

                                             ▼
                                 Snowflake: external_signals
                                    company_signal_summaries

  ┌─────────────────────┬───────────────────────────────┬───────────────────────────────────────────┐
  │       Signal        │            Source             │             What it measures              │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ technology_hiring   │ JobSpy (LinkedIn/Indeed)      │ AI job ratio within tech postings         │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ innovation_activity │ PatentsView / USPTO           │ AI patent ratio + recency                 │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ digital_presence    │ BuiltWith + Wappalyzer + Groq │ AI tools in tech stack                    │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ leadership_signals  │ SEC DEF-14A proxy filings     │ Tech exec presence + AI strategy keywords │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ board_composition   │ SEC proxy (board bios)        │ Board-level tech expertise                │
  ├─────────────────────┼───────────────────────────────┼───────────────────────────────────────────┤
  │ culture             │ Glassdoor reviews (S3)        │ Culture indicators for AI readiness       │
  └─────────────────────┴───────────────────────────────┴───────────────────────────────────────────┘

  Each signal uses a weighted sub-scoring formula (ratio + volume + diversity components)
  then persists to Snowflake.

  All signal services live in app/services/signals/ (10 files including base class).

  ━━━━━━━━━━━━━━━━━━━━━━ CS3: DIMENSION SCORING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Evidence-to-Dimension Mapping
  ─────────────────────────────
  The Evidence Mapper (evidence_mapper.py) implements the Table 1 Matrix: each of the
  9 evidence sources (6 signals + 3 SEC section rubric scores) distributes its score
  across the 7 V^R dimensions using primary/secondary weights.

  The Rubric Scorer (rubric_scorer.py) processes raw SEC text (Items 1, 1A, 7)
  through 5-level keyword-matching rubrics to produce 3 additional input scores.

  ╔═══════════════════════════════════════════════════════╗
  ║         EVIDENCE MAPPER (Table 1 Matrix)              ║
  ║              evidence_mapper.py                       ║
  ║                                                       ║
  ║  9 sources × weights ──────────────────────────────►  ║
  ║                                                       ║
  ║  ┌─────────────────────┬──────────┬─────────────┐     ║
  ║  │ Source              │ Primary  │ Weight      │     ║
  ║  ├─────────────────────┼──────────┼─────────────┤     ║
  ║  │ technology_hiring   │ talent   │ 0.70        │     ║
  ║  │ innovation_activity │ tech_stk │ 0.50        │     ║
  ║  │ digital_presence    │ data_inf │ 0.60        │     ║
  ║  │ leadership_signals  │ leader   │ 0.60        │     ║
  ║  │ sec_item_1          │ use_case │ 0.70        │     ║
  ║  │ sec_item_1a         │ ai_gov   │ 0.80        │     ║
  ║  │ sec_item_7          │ leader   │ 0.50        │     ║
  ║  │ glassdoor_reviews   │ culture  │ 0.80        │     ║
  ║  │ board_composition   │ ai_gov   │ 0.70        │     ║
  ║  └─────────────────────┴──────────┴─────────────┘     ║
  ╚═══════════════════════════════════════════════════════╝

                    ▼   (each source also distributes to secondary dimensions)

  7 DIMENSION SCORES [0-100]
  ───────────────────────────────────────────────────────────────────────────
  data_infrastructure  (w=0.18) ◄── digital_presence (0.60), innovation (0.20)
  talent_skills        (w=0.17) ◄── technology_hiring (0.70)
  ai_governance        (w=0.15) ◄── sec_item_1a (0.80), board_composition (0.70)
  technology_stack     (w=0.15) ◄── innovation (0.50), digital_presence (0.40)
  leadership_vision    (w=0.13) ◄── leadership_signals (0.60), sec_item_7 (0.50)
  use_case_portfolio   (w=0.12) ◄── sec_item_1 (0.70), innovation (0.30)
  culture_change       (w=0.10) ◄── glassdoor_reviews (0.80)

  ━━━━━━━━━━━━━━━━━━━━━ CS3: COMPOSITE SCORING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  6-step scoring cascade (all calculators in app/scoring/):

    ┌─────────────────────────────────────────────────────────────────────────────┐
    │  STEP 1: Talent Concentration (TC)          talent_concentration.py         │
    │                                                                              │
    │  Jobs + Glassdoor ──► TC = 1 / √(1 + seniority_ratio + skill_concentration) │
    │                        TC ∈ [0, 1]  (0=distributed, 1=concentrated)         │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
    ┌──────────────────────────────────────────▼──────────────────────────────────┐
    │  STEP 2: V^R (Venture Readiness)             vr_calculator.py               │
    │                                                                              │
    │  7 dim scores × weights ──► base_VR                                         │
    │  base_VR × TalentRiskAdj  (penalty if TC > 0.25: ×(1 - 0.15×(TC-0.25)))   │
    │         × CVPenalty       (non-compensatory: ×(1 - 0.25×CV))               │
    │  ──► V^R ∈ [0, 100]                                                         │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
    ┌──────────────────────────────────────────▼──────────────────────────────────┐
    │  STEP 3: Position Factor (PF)                position_factor.py             │
    │                                                                              │
    │  Market cap percentile ──► PF = 2 × (percentile - 0.5)                     │
    │  NVDA(95th)→+0.90  JPM(85th)→+0.70  GE(50th)→0.00  DG(30th)→-0.40        │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
    ┌──────────────────────────────────────────▼──────────────────────────────────┐
    │  STEP 4: H^R (Human Readiness)               hr_calculator.py              │
    │                                                                              │
    │  Sector baseline × (1 + 0.15 × PF)                                          │
    │  tech=84, finance=68, manufacturing=52, retail=55                           │
    │  ──► H^R ∈ [0, 100]                                                         │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
    ┌──────────────────────────────────────────▼──────────────────────────────────┐
    │  STEP 5: Synergy                             synergy_calculator.py          │
    │                                                                              │
    │  Alignment = 1 - |V^R - H^R| / 100                                          │
    │  TimingFactor: tech=1.20, finance=1.05, retail/mfg=1.00                    │
    │  Synergy = (V^R × H^R / 100) × Alignment × TimingFactor                   │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
    ┌──────────────────────────────────────────▼──────────────────────────────────┐
    │  STEP 6: Org-AI-R Final Score                orgair_calculator.py           │
    │                                                                              │
    │  Org-AI-R = 0.88 × [0.60 × V^R + 0.40 × H^R] + 0.12 × Synergy           │
    │           = (1-β) × [α×VR + (1-α)×HR] + β×Synergy                         │
    │           α=0.60, β=0.12                                                    │
    └──────────────────────────────────────────┬──────────────────────────────────┘
                                               │
                                               ▼
                            ┌─────────────────────────────────┐
                            │         FINAL OUTPUT             │
                            │  { ticker, org_air_score,        │
                            │    vr_score, hr_score,           │
                            │    synergy, tc, pf,              │
                            │    dimension_scores }            │
                            │         ▼         ▼              │
                            │    Snowflake      S3 JSON        │
                            └─────────────────────────────────┘

  Constants: α=0.60 (V^R weight), β=0.12 (synergy weight)
  The 6-step cascade lives in composite_scoring_service.py which orchestrates calls
  to the individual calculator modules in app/scoring/.

  ━━━━━━━━━━━━━━━━━━━━━━━━ CS4: RAG & RETRIEVAL ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Evidence Indexing
  ─────────────────

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/rag/index/{ticker}                                             │
  │                                                                              │
  │  Snowflake chunks + S3 signals                                               │
  │        │                                                                     │
  │        ├──► VectorStore (Chroma Cloud HTTP)                                  │
  │        │     ├─ Dense embedding (sentence-transformers, all-MiniLM-L6-v2)    │
  │        │     ├─ Multi-dim indexing: each chunk indexed per relevant dim       │
  │        │     │   (weight >= 0.15, ~1.5-2x doc expansion)                     │
  │        │     └─ Client-side metadata filtering (ticker, dimension)           │
  │        │                                                                     │
  │        └──► BM25 Sparse Index                                                │
  │              ├─ Seeded from CS2Evidence objects (seed_from_evidence)          │
  │              ├─ Pickle cache with 24hr TTL                                   │
  │              └─ Multi-seed query approach (~500 doc limit)                    │
  └──────────────────────────────────────────────────────────────────────────────┘

  Hybrid Retrieval
  ────────────────

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  HybridRetriever (hybrid.py)                                                 │
  │                                                                              │
  │  Query ──┬──► Dense Search (VectorStore.search)  ──┐                        │
  │          │                                          ├──► RRF Fusion          │
  │          └──► Sparse Search (BM25Okapi)         ──┘    (dense=0.6,          │
  │                                                         sparse=0.4)          │
  │                                                          │                   │
  │                                                          ▼                   │
  │                                              RetrievedDocument list          │
  └──────────────────────────────────────────────────────────────────────────────┘

  Justification Generation
  ────────────────────────

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  GET /api/v1/rag/justify/{ticker}/{dimension}                                │
  │                                                                              │
  │  1. Fetch dimension score ──────────────────────── ScoringRepository         │
  │  2. Get rubric criteria ────────────────────────── CS3Client (static data)   │
  │  3. Build search query from rubric keywords                                  │
  │  4. Retrieve evidence ──────────────────────────── HybridRetriever           │
  │  5. Match evidence to rubric keywords ──────────── CitedEvidence objects     │
  │  6. Identify gaps (next-level criteria not met)                              │
  │  7. LLM generation ────────────────────────────── ModelRouter (DeepSeek)     │
  │                                                                              │
  │  Output: ScoreJustification                                                  │
  │    ├─ confidence_interval (from DB)                                          │
  │    ├─ evidence_strength (strong/moderate/weak)                               │
  │    ├─ supporting_evidence (CitedEvidence list)                               │
  │    └─ gaps_identified (next-level criteria)                                  │
  └──────────────────────────────────────────────────────────────────────────────┘

  Guardrails
  ──────────

  ┌──────────────────────┬──────────────────────────────────────────────────────┐
  │  INPUT GUARDS         │  validate_ticker (regex, uppercase 1-10 chars)      │
  │  input_guards.py      │  validate_question (10-500 chars, injection detect) │
  │                       │  validate_dimension (against DIMENSIONS enum)       │
  ├──────────────────────┼──────────────────────────────────────────────────────┤
  │  OUTPUT GUARDS        │  check_answer_length (20-2000 chars)                │
  │  output_guards.py     │  check_answer_grounded (disclaimer if no evidence) │
  │                       │  check_no_refusal (fallback for LLM refusals)      │
  └──────────────────────┴──────────────────────────────────────────────────────┘

  Chatbot & IC Prep
  ─────────────────

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  GET /api/v1/rag/chatbot/{ticker}?q=...                                      │
  │                                                                              │
  │  Question ──► Input Guards ──► HybridRetriever ──► Claude Haiku (LLM)       │
  │                                                         │                    │
  │                                                         ▼                    │
  │                                              Output Guards ──► Answer        │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  GET /api/v1/rag/ic-prep/{ticker}          ic_prep.py                        │
  │                                                                              │
  │  1. Fetch company metadata from DB                                           │
  │  2. Generate 7 dimension justifications in parallel (asyncio.gather)         │
  │  3. Identify strengths (level >= 4), gaps (level <= 2), risk factors         │
  │  4. Generate IC recommendation: PROCEED / CAUTION / FURTHER DILIGENCE       │
  │                                                                              │
  │  Output: IC package (7 justifications + strengths + gaps + recommendation)   │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━━━ CS5: MULTI-AGENT DUE DILIGENCE ━━━━━━━━━━━━━━━━━━━━━━━━━

  LangGraph Agent Workflow
  ────────────────────────

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  POST /api/v1/dd/run/{ticker}                                                │
  │  assessment_type: screening | limited | full                                 │
  │                                                                              │
  │  DueDiligenceState (TypedDict):                                              │
  │    company_id, assessment_type, requested_by                                 │
  │    sec_analysis, talent_analysis, scoring_result                             │
  │    evidence_justifications, value_creation_plan                              │
  │    next_agent, requires_approval, approval_status                            │
  │    messages: List[AgentMessage]  (append-only via operator.add)              │
  │                                                                              │
  │  Checkpointing: MemorySaver (thread-based, supports HITL resume)             │
  └──────────────────────────────────────────────────────────────────────────────┘

                                      │
                                      ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │  SUPERVISOR NODE (conditional router — sequential stage dispatch)       │
    │  supervisor.py                                                          │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  1. SEC ANALYST NODE                                         [always]   │
    │     └─ MCPToolCaller.get_company_evidence(limit=10)                     │
    │     └─ LLM summarise (ModelRouter, task: "evidence_extraction")         │
    │     └─ Optional: Mem0 context recall (cross-session memory)             │
    │     ──► writes state["sec_analysis"]                                    │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  2. SCORER NODE                                              [always]   │
    │     └─ MCPToolCaller.calculate_org_air_score()                          │
    │        └─ CompositeScoringRepository (SCORING table)                    │
    │     └─ HITL gate if org_air > 85 or < 40                               │
    │     ──► writes state["scoring_result"], sets requires_approval          │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  3. EVIDENCE NODE                              [limited | full only]     │
    │     └─ MCPToolCaller.get_company_evidence (3 dims:                      │
    │        data_infrastructure, talent, use_case_portfolio)                  │
    │     └─ Filter to technology_hiring signals                              │
    │     └─ LLM summarise                                                    │
    │     ──► writes state["talent_analysis"] + state["evidence_justif..."]   │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  4. VALUE CREATOR NODE                              [full only]          │
    │     ├─ MCPToolCaller.run_gap_analysis()                                 │
    │     │   └─ GapAnalyzer -> dimension gaps + priorities + actions         │
    │     ├─ MCPToolCaller.project_ebitda_impact()                            │
    │     │   └─ EBITDACalculator -> scenarios {conservative, base, optimistic│
    │     ├─ HITL gate if risk_adjusted EBITDA >= 50%                         │
    │     └─ LLM narrative (ModelRouter, task: "ic_summary")                  │
    │     ──► writes state["value_creation_plan"], sets requires_approval     │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  5. HITL APPROVAL NODE                         [if requires_approval]   │
    │     └─ interrupt(payload) ──► graph pauses                              │
    │     └─ Resume: POST /api/v1/dd/approve/{thread_id}                     │
    │        └─ Command(resume={decision, approved_by, comments})             │
    └────────────────────────────────────┬────────────────────────────────────┘
                                         │
    ┌────────────────────────────────────▼────────────────────────────────────┐
    │  6. COMPLETE NODE                                                       │
    │     └─ set completed_at ──► END                                         │
    │     └─ Record AssessmentSnapshot (history_service)                      │
    │     └─ Store Mem0 semantic memory                                       │
    └─────────────────────────────────────────────────────────────────────────┘

  Mem0 Semantic Memory (app/agents/memory.py)
  ───────────────────────────────────────────
  Optional cross-session recall for agents. Stores assessment summaries
  (org_air, vr, hr scores + top gaps + narrative) keyed by company ticker.
  Lazy-initializes via mem0ai library. Falls back to Groq/Anthropic for LLM
  backend. Fully no-op if MEM0_API_KEY is not set.

  ━━━━━━━━━━━━━━━━━━━━━━━ CS5: MCP TOOL SERVER ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  MCP stdio server (app/mcp/server.py) — 6 tools, 2 resources, 2 prompts
  Lazy init: expensive clients (Snowflake, ChromaDB) only on first tool call

  Tools:
  ┌──────────────────────┬─────────────────────────────┬──────────────────────────────────────┐
  │       Tool           │          Source              │             Purpose                   │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ calculate_org_air_   │ SCORING table (Snowflake)   │ Org-AI-R + V^R + H^R + dims + CI     │
  │   score              │                             │                                       │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ get_company_         │ /rag/evidence/{ticker}      │ Evidence items (optional dim filter)   │
  │   evidence           │  (FastAPI HTTP)             │                                       │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ generate_            │ /rag/justify/{ticker}/{dim} │ Score justification with gaps          │
  │   justification      │  (FastAPI HTTP)             │                                       │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ project_ebitda_      │ EBITDACalculator (local)    │ Sector-adjusted EBITDA delta           │
  │   impact             │                             │  + HR risk factor + scenarios          │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ run_gap_analysis     │ GapAnalyzer (local)         │ Dimension gaps, priorities, actions    │
  ├──────────────────────┼─────────────────────────────┼──────────────────────────────────────┤
  │ get_portfolio_       │ CS3_PORTFOLIO iteration     │ Fund-AI-R, per-company summary         │
  │   summary            │                             │                                       │
  └──────────────────────┴─────────────────────────────┴──────────────────────────────────────┘

  Resources:
    orgair://parameters/v2.0  — Scoring parameters (alpha, beta, dimension weights)
    orgair://sectors           — Sector baselines and timing factors

  Prompts:
    due_diligence_assessment  — Full DD assessment prompt template
    ic_meeting_prep           — IC meeting preparation prompt template

  MCPToolCaller (agents/specialists.py) mirrors MCP tool dispatch via HTTP
  ──► single source of truth for business logic

  ━━━━━━━━━━━━━━━━━━━━━━ CS5: VALUE CREATION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  EBITDACalculator (ebitda.py)                                                │
  │                                                                              │
  │  score_improvement = target_score - entry_score                              │
  │                                                                              │
  │  Sector multipliers (bps per Org-AI-R point):                                │
  │    Tech=0.45  FinServ=0.38  Healthcare=0.35  Mfg=0.30  Retail=0.28           │
  │                                                                              │
  │  Implementation cost (% revenue per point):                                  │
  │    Tech=0.08  FinServ=0.12  Healthcare=0.15  etc.                            │
  │                                                                              │
  │  ebitda_impact = score_improvement x sector_multiplier                       │
  │  impl_cost    = score_improvement x cost_factor                              │
  │  net_impact   = ebitda_impact - impl_cost                                    │
  │                                                                              │
  │  H^R Risk Adjustment:                                                        │
  │    hr_risk_factor = min(1.0, max(0.5, h_r_score / 80))                       │
  │    H^R > 70 -> low risk (minimal adjustment)                                 │
  │    H^R < 50 -> high risk (50% floor)                                         │
  │                                                                              │
  │  adjusted_net = net_impact x hr_risk_factor                                  │
  │                                                                              │
  │  Time-to-Value: <=10pt=12mo, <=20pt=18mo, <=30pt=24mo, >30pt=36mo            │
  │  Confidence: <=15pt + H^R>=60 -> high, <=25pt -> medium, >25pt -> low        │
  │                                                                              │
  │  Output: EBITDAProjection                                                    │
  │    ├─ scenarios {conservative, base, optimistic}                             │
  │    ├─ time_to_value_months                                                   │
  │    └─ confidence {high, medium, low}                                         │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  GapAnalyzer (gap_analysis.py)                                               │
  │                                                                              │
  │  Improvement priority:                                                       │
  │    data_infrastructure (1) -> talent (2) -> technology_stack (3)             │
  │    -> ai_governance (4) -> leadership (5) -> use_case_portfolio (6)          │
  │    -> culture (7)                                                            │
  │                                                                              │
  │  For each dimension:                                                         │
  │    current_score -> target_score (proportional to total gap)                 │
  │    gap = target - current                                                    │
  │    next-level criteria from CS3Client static rubric                          │
  │    improvement actions from per-dimension/level actions_map                  │
  │                                                                              │
  │  Output: GapAnalysisResult                                                   │
  │    ├─ dimensions (sorted by gap size)                                        │
  │    └─ top_priorities (top 3 by gap)                                          │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━━━━━ CS5: REPORTING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  ICMemoGenerator (ic_memo.py)                                                │
  │                                                                              │
  │  Output format chain:                                                        │
  │    1. .docx (python-docx) ── primary format                                  │
  │    2. .pdf  (WeasyPrint)  ── fallback if docx unavailable                    │
  │    3. .txt  (plain text)  ── final fallback                                  │
  │                                                                              │
  │  Includes:                                                                   │
  │    ├─ Org-AI-R, V^R, H^R scores                                              │
  │    ├─ 7 dimension score breakdown                                            │
  │    ├─ Gap analysis results + 100-day plan                                    │
  │    ├─ EBITDA scenario projections                                            │
  │    ├─ Recommendation: PROCEED / MONITOR / CONDITIONAL                        │
  │    └─ Confidential marking + colored recommendation boxes                    │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  LPLetterGenerator (lp_letter.py)                                            │
  │                                                                              │
  │  Output format chain:                                                        │
  │    1. .pdf  (WeasyPrint HTML-to-PDF) ── primary format                       │
  │    2. .txt  (plain text)             ── fallback                             │
  │                                                                              │
  │  Includes:                                                                   │
  │    ├─ Quarterly AI readiness update header                                   │
  │    ├─ Fund-AI-R score and portfolio composition                              │
  │    ├─ AI leaders count (>=70) and laggards count (<50)                        │
  │    ├─ Portfolio highlights (top companies with scores)                       │
  │    └─ Next steps and outlook                                                 │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━ CS5: ANALYTICS & TRACKING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  FundAIRCalculator (fund_air.py)                                             │
  │                                                                              │
  │  Fund-AI-R = EV-weighted average of portfolio Org-AI-R scores                │
  │                                                                              │
  │  Output: CS5FundMetrics                                                      │
  │    ├─ fund_air (composite fund score)                                        │
  │    ├─ company_count                                                          │
  │    ├─ quartile_distribution (sector-specific benchmarks)                     │
  │    ├─ sector_hhi (concentration index)                                       │
  │    ├─ ai_leaders (org_air >= 70)                                             │
  │    ├─ ai_laggards (0 < org_air < 50)                                         │
  │    ├─ avg_delta_since_entry                                                  │
  │    └─ total_ev_mm                                                            │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  AssessmentHistoryService (history_service.py)                               │
  │                                                                              │
  │  AssessmentSnapshot: point-in-time capture                                   │
  │    (org_air, vr, hr, synergy, dimension_scores, confidence_interval)         │
  │                                                                              │
  │  AssessmentTrend: current org_air, entry org_air                             │
  │    delta_since_entry, delta_30d, delta_90d, trend_direction                  │
  │                                                                              │
  │  In-memory cache (max 100 companies, 50 snapshots each)                      │
  │  Optional Snowflake persistence via AssessmentSnapshotRepository             │
  └──────────────────────────────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  InvestmentTracker (investment_tracker.py)                                   │
  │                                                                              │
  │  ROI computation:                                                            │
  │    revenue_lift (%) + EBITDA_lift (%) + multiple_expansion (%)               │
  │                                                                              │
  │  Output: PortfolioROI                                                        │
  │    (total_projected_return_mm, roi_pct, scenario breakdown)                  │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━━━━━ CS5: STREAMLIT DASHBOARD ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Entry: streamlit run streamlit/cs5_app.py  ->  http://localhost:8501

  ┌──────────────────────────────────────────────────────────────────────────────┐
  │  Sidebar                                                                     │
  │    ├─ fetch_available_companies() -> GET /api/v1/companies/all               │
  │    ├─ Portfolio multiselect (session_state)                                  │
  │    └─ DD runner (assessment_type selector)                                   │
  │                                                                              │
  │  Page 1: Portfolio Overview                                                  │
  │    ├─ Fund-AI-R metrics (overall fund score, company count)                 │
  │    ├─ Scatter: org_air vs sector                                             │
  │    ├─ Bar: quartile distribution                                             │
  │    └─ Company metrics table (ticker, sector, scores, deltas)                │
  │                                                                              │
  │  Page 2: Evidence Analysis                                                   │
  │    ├─ 7 justification panels per company                                     │
  │    ├─ Evidence cards (evidence_display.py)                                   │
  │    └─ Dimension table (score, level, confidence_interval)                    │
  │                                                                              │
  │  Page 3: Assessment History                                                  │
  │    ├─ Historical snapshot trends                                             │
  │    └─ Delta tracking (30d, 90d, since entry)                                │
  │                                                                              │
  │  Page 4: Agentic Workflow                                                    │
  │    ├─ Assessment type selector {screening, limited, full}                    │
  │    ├─ POST /api/v1/dd/run/{ticker}                                           │
  │    ├─ Monitor execution (poll status)                                        │
  │    └─ Approval dialog (if paused at HITL gate)                               │
  │                                                                              │
  │  Page 5: Fund-AI-R Analytics                                                 │
  │    ├─ Fund-level metrics                                                     │
  │    ├─ Sector concentration (HHI)                                             │
  │    └─ Leaders / laggards breakdown                                           │
  │                                                                              │
  │  Page 6: MCP Server                                                          │
  │    ├─ Tool documentation                                                     │
  │    └─ Live tool testing interface                                            │
  │                                                                              │
  │  Page 7: Prometheus Metrics                                                  │
  │    └─ Counter / histogram visualization from /metrics endpoint               │
  │                                                                              │
  │  Page 8: IC Memo / LP Letter                                                 │
  │    ├─ IC memo generation (docx/pdf/txt)                                      │
  │    ├─ LP letter generation (pdf/txt)                                         │
  │    └─ Download buttons                                                       │
  │                                                                              │
  │  Page 9: Investment Tracker                                                  │
  │    └─ ROI projections (revenue lift, EBITDA lift, multiple expansion)        │
  │                                                                              │
  │  Page 10: Mem0 Memory                                                        │
  │    └─ Semantic memory viewer (cross-session agent recall)                    │
  └──────────────────────────────────────────────────────────────────────────────┘

  ━━━━━━━━━━━━━━━━━━━━ CS5: DATA STORES & OBSERVABILITY ━━━━━━━━━━━━━━━━━━━━━━━

  ┌─────────────┬──────────────────────────────────────────────────────────────┐
  │  Store      │  Role                                                        │
  ├─────────────┼──────────────────────────────────────────────────────────────┤
  │  Snowflake  │  Companies, documents, signals, scores, assessments          │
  ├─────────────┼──────────────────────────────────────────────────────────────┤
  │  Redis      │  Response cache (5-min TTL) + background task state          │
  ├─────────────┼──────────────────────────────────────────────────────────────┤
  │  AWS S3     │  Raw/parsed/chunked SEC filings, signal data, results JSON   │
  ├─────────────┼──────────────────────────────────────────────────────────────┤
  │  ChromaDB   │  Dense vector store + BM25 sparse index for RAG              │
  ├─────────────┼──────────────────────────────────────────────────────────────┤
  │  Mem0       │  Optional semantic memory for cross-session agent recall     │
  └─────────────┴──────────────────────────────────────────────────────────────┘

  Observability:
    ├─ Prometheus counters/histograms (@track_mcp_tool, @track_agent decorators)
    ├─ structlog with JSON format + correlation ID on every log entry
    ├─ X-Correlation-ID header on all HTTP requests/responses
    ├─ Health endpoints: GET /healthz (liveness), GET /health (dependency check)
    └─ Metrics endpoint: GET /metrics (Prometheus scrape)

  ━━━━━━━━━━━━━━━━━━━━━━━ LLM ROUTING ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ModelRouter (app/services/llm/router.py) — LiteLLM abstraction, $5/day budget

  ┌──────────────────┬──────────────────────────────────┬──────────────────────┐
  │  Provider        │  Tasks                           │  Cost                │
  ├──────────────────┼──────────────────────────────────┼──────────────────────┤
  │  Groq (FREE)     │  evidence_extraction,            │  ~$0.00/1K tokens    │
  │  llama-3.1-8b    │  keyword_matching,               │                      │
  │  llama-3.3-70b   │  hyde_generation,                │                      │
  │                  │  governance_extraction,           │                      │
  │                  │  tech_stack_fallback              │                      │
  ├──────────────────┼──────────────────────────────────┼──────────────────────┤
  │  Claude Haiku    │  justification_generation,       │  $0.00125/1K tokens  │
  │  4.5 (Anthropic) │  ic_summary,                     │  ~4000 chatbot       │
  │                  │  chat_response                   │  answers per $5      │
  ├──────────────────┼──────────────────────────────────┼──────────────────────┤
  │  DeepSeek        │  Fallback for all tasks          │  $0.00014/1K tokens  │
  │  deepseek-chat   │  (if Groq + Claude fail)         │                      │
  └──────────────────┴──────────────────────────────────┴──────────────────────┘

  Embeddings: SentenceTransformers all-MiniLM-L6-v2 (local, no API cost)

  ━━━━━━━━━━━━━━━━━━━━━ COMPLETE DIRECTORY STRUCTURE ━━━━━━━━━━━━━━━━━━━━━━━━━━

  pe-org-air-platform/
  ├── app/
  │   ├── main.py                          # FastAPI entry point (19 routers registered)
  │   ├── shutdown.py                      # Graceful shutdown event
  │   │
  │   ├── core/
  │   │   ├── dependencies.py              # Singleton DI providers
  │   │   ├── errors.py                    # PlatformError hierarchy
  │   │   ├── lifespan.py                  # Startup init + shutdown hooks
  │   │   ├── logging_config.py            # structlog configuration
  │   │   └── settings.py                  # Settings base
  │   │
  │   ├── middleware/
  │   │   └── correlation.py               # X-Correlation-ID middleware
  │   │
  │   ├── config/
  │   │   ├── __init__.py                  # App settings (Snowflake, Redis, S3, LLM keys)
  │   │   ├── company_mappings.py          # CS3_PORTFOLIO, COMPANY_NAME_MAPPINGS, CompanyRegistry
  │   │   └── retrieval_settings.py        # RAG tunables (max_context_chars, etc.)
  │   │
  │   ├── routers/                         # 18 HTTP endpoint routers
  │   │   ├── health.py                    # /healthz, /health (deep check)
  │   │   ├── config.py                    # Scoring params, weights, baselines
  │   │   ├── companies.py                 # CS1: Company CRUD + Groq enrichment
  │   │   ├── portfolios.py                # CS1: Portfolio management
  │   │   ├── documents.py                 # CS2: SEC collection / parsing / chunking
  │   │   ├── signals.py                   # CS2: Signal collection (6 categories)
  │   │   ├── evidence.py                  # CS2: Evidence summary
  │   │   ├── dimension_scores.py          # CS3: Dimension weight config
  │   │   ├── scoring.py                   # CS3: Dimension scoring pipeline
  │   │   ├── tc_vr_scoring.py             # CS3: Talent Concentration + V^R
  │   │   ├── position_factor.py           # CS3: Position Factor
  │   │   ├── hr_scoring.py                # CS3: Human Readiness H^R
  │   │   ├── orgair_scoring.py            # CS3: Org-AI-R (+ assessment_router secondary)
  │   │   ├── rag.py                       # CS4: ChromaDB indexing + RAG + chatbot
  │   │   ├── analyst_notes.py             # CS4: Post-LOI DD notes
  │   │   ├── bonus.py                     # CS4: Bonus analysis
  │   │   ├── history.py                   # CS4: Assessment history tracking
  │   │   └── due_diligence.py             # CS5: LangGraph DD workflow + HITL
  │   │
  │   ├── services/
  │   │   ├── signals/                     # Signal collection services
  │   │   │   ├── base_signal_service.py   # Base interface
  │   │   │   ├── job_signal_service.py    # JobSpy → technology_hiring
  │   │   │   ├── job_data_service.py      # Job data collection
  │   │   │   ├── patent_signal_service.py # PatentsView → innovation_activity
  │   │   │   ├── tech_signal_service.py   # BuiltWith → digital_presence
  │   │   │   ├── leadership_service.py    # DEF-14A → leadership_signals
  │   │   │   ├── board_composition_service.py # Board analysis → board_composition
  │   │   │   ├── culture_signal_service.py # Glassdoor → culture
  │   │   │   └── evidence_service.py      # Evidence aggregation
  │   │   │
  │   │   ├── search/
  │   │   │   └── vector_store.py          # ChromaDB client + EMBEDDING_MODEL
  │   │   │
  │   │   ├── retrieval/
  │   │   │   ├── hybrid.py                # BM25 + dense hybrid retrieval
  │   │   │   ├── dimension_mapper.py      # Evidence → V^R dimension mapping
  │   │   │   └── hyde.py                  # HyDE hypothetical doc embeddings
  │   │   │
  │   │   ├── justification/
  │   │   │   └── generator.py             # LLM-based score justification
  │   │   │
  │   │   ├── llm/
  │   │   │   └── router.py                # ModelRouter (Groq/DeepSeek/Claude via LiteLLM)
  │   │   │
  │   │   ├── integration/                 # CS5 integration clients
  │   │   │   ├── cs1_client.py            # Company metadata (Sector enum, Company dataclass)
  │   │   │   ├── cs2_client.py            # S3 evidence + Groq keyword expansion
  │   │   │   ├── cs3_client.py            # Scoring (DimensionScore, CompanyAssessment)
  │   │   │   └── cs4_client.py            # RAG search + JustificationResult
  │   │   │
  │   │   ├── value_creation/
  │   │   │   ├── ebitda.py                # EBITDA projections (sector multipliers)
  │   │   │   └── gap_analysis.py          # Dimension gap analysis + improvement actions
  │   │   │
  │   │   ├── reporting/
  │   │   │   ├── ic_memo.py               # IC memo (.docx/.pdf/.txt)
  │   │   │   └── lp_letter.py             # LP letter (.pdf/.txt)
  │   │   │
  │   │   ├── tracking/
  │   │   │   ├── history_service.py       # Assessment snapshots + trends
  │   │   │   └── investment_tracker.py    # ROI computation
  │   │   │
  │   │   ├── analytics/
  │   │   │   └── fund_air.py              # Fund-AI-R calculator (EV-weighted)
  │   │   │
  │   │   ├── observability/
  │   │   │   └── metrics.py               # Prometheus counters/histograms
  │   │   │
  │   │   ├── workflows/
  │   │   │   └── ic_prep.py               # IC meeting package workflow
  │   │   │
  │   │   ├── collection/
  │   │   │   └── analyst_notes.py         # AnalystNotesCollector
  │   │   │
  │   │   ├── document_collector.py        # SEC EDGAR download + S3 upload
  │   │   ├── document_parsing_service.py  # HTML/PDF text extraction
  │   │   ├── document_chunking_service.py # Overlapping chunk generation
  │   │   ├── scoring_service.py           # CS3 rubric scoring + dimension mapping
  │   │   ├── composite_scoring_service.py # TC, V^R, PF, H^R, Org-AI-R orchestrator
  │   │   ├── groq_enrichment.py           # Groq LLM metadata enrichment
  │   │   ├── portfolio_data_service.py    # Unified CS1-CS4 facade
  │   │   ├── cache.py                     # Redis TTL cache helpers
  │   │   ├── redis_cache.py               # Redis client wrapper
  │   │   ├── s3_storage.py                # AWS S3 client wrapper
  │   │   └── task_store.py                # Redis-backed background task state
  │   │
  │   ├── agents/
  │   │   ├── state.py                     # DueDiligenceState TypedDict + AgentMessage
  │   │   ├── specialists.py               # MCPToolCaller + 4 specialist agents
  │   │   ├── supervisor.py                # LangGraph StateGraph factory + HITL node
  │   │   └── memory.py                    # Mem0 semantic memory (optional)
  │   │
  │   ├── mcp/
  │   │   ├── server.py                    # MCP stdio server (6 tools, 2 resources, 2 prompts)
  │   │   └── test_mcp.py                  # MCP integration tests
  │   │
  │   ├── pipelines/
  │   │   ├── sec_edgar.py                 # SEC EDGAR filing collector
  │   │   ├── job_signals.py               # Job posting signal processing
  │   │   ├── patent_signals.py            # Patent signal processing
  │   │   ├── tech_signals.py              # Technology signal analysis
  │   │   ├── leadership_analyzer.py       # Leadership analysis
  │   │   ├── board_analyzer.py            # Board composition analysis
  │   │   ├── board_io.py                  # Board data I/O
  │   │   ├── glassdoor_collector.py       # Glassdoor culture data
  │   │   ├── document_parser.py           # Multi-format document parsing
  │   │   ├── chunking.py                  # Chunking utilities
  │   │   ├── keywords.py                  # Keyword management
  │   │   ├── signal_pipeline_state.py     # Pipeline state management
  │   │   └── utils.py                     # Pipeline utilities
  │   │
  │   ├── repositories/                    # 11 Snowflake data access modules
  │   │   ├── base.py                      # RepositoryException + connection base
  │   │   ├── company_repository.py
  │   │   ├── industry_repository.py
  │   │   ├── document_repository.py
  │   │   ├── chunk_repository.py
  │   │   ├── signal_repository.py
  │   │   ├── scoring_repository.py
  │   │   ├── composite_scoring_repository.py
  │   │   ├── dimension_score_repository.py
  │   │   ├── assessment_repository.py
  │   │   ├── assessment_snapshot_repository.py
  │   │   └── health_repository.py
  │   │
  │   ├── models/
  │   │   ├── dimension.py                 # DIMENSION_WEIGHTS dict
  │   │   ├── document.py                  # Document model
  │   │   ├── enumerations.py              # DIMENSION_ALIAS_MAP
  │   │   ├── evidence.py                  # Evidence model
  │   │   └── signal.py                    # Signal model
  │   │
  │   ├── schemas/
  │   │   ├── evidence.py                  # Evidence response schema
  │   │   ├── portfolio.py                 # Portfolio response schema
  │   │   └── scoring.py                   # Scoring response schema
  │   │
  │   ├── scoring/                         # 10 calculation modules
  │   │   ├── evidence_mapper.py           # Evidence-to-dimension mapping (Table 1)
  │   │   ├── rubric_scorer.py             # 5-level keyword rubric engine
  │   │   ├── talent_concentration.py      # TC calculation
  │   │   ├── vr_calculator.py             # V^R calculation
  │   │   ├── position_factor.py           # PF calculation
  │   │   ├── hr_calculator.py             # H^R calculation
  │   │   ├── synergy_calculator.py        # Synergy calculation
  │   │   ├── orgair_calculator.py         # Final Org-AI-R score
  │   │   ├── confidence_calculator.py     # Confidence interval
  │   │   └── utils.py                     # Scoring utilities
  │   │
  │   ├── guardrails/
  │   │   ├── input_guards.py              # Ticker, question, dimension validation
  │   │   └── output_guards.py             # Answer length, grounding, refusal checks
  │   │
  │   ├── prompts/
  │   │   └── rag_prompts.py               # Chatbot + dimension detection prompts
  │   │
  │   ├── clients/
  │   │   └── base.py                      # BaseAPIClient (async HTTP + backoff)
  │   │
  │   ├── utils/
  │   │   ├── company_resolver.py          # Ticker/name/CIK resolution
  │   │   ├── id_utils.py                  # ID generation
  │   │   └── serialization.py             # JSON serialization helpers
  │   │
  │   ├── scripts/                         # Utility / maintenance scripts
  │   │   ├── collect_evidence.py          # Evidence collection
  │   │   ├── ensure_portfolio.py          # Portfolio validation
  │   │   ├── fill_sectors.py              # Sector population
  │   │   ├── seed_history.py              # History seeding
  │   │   ├── query_snowflake.py           # Snowflake query utility
  │   │   ├── recalc_culture.py            # Culture score recalculation
  │   │   ├── reingest_10k.py              # 10-K reingestion
  │   │   ├── diagnose_ge_sections.py      # GE diagnostics
  │   │   └── test_connections.py          # Connection tests
  │   │
  │   └── database/                        # SQL schemas + seed data
  │       ├── schema.sql                   # Main application schema
  │       ├── document_schema.sql
  │       ├── document_chunks_schema.sql
  │       ├── signals_schema.sql
  │       ├── digital_presence_schema.sql
  │       ├── scoring_runs_schema.sql
  │       ├── final_scoring_schema.sql
  │       ├── final_notes.sql
  │       ├── signal_mapping_dimensions.sql
  │       ├── seed-companies.sql           # 5 portfolio companies
  │       ├── seed-documents.sql
  │       ├── seed-dimension-scores.sql
  │       ├── seed-assessments.sql
  │       └── seed-industries.sql
  │
  ├── streamlit/                           # Portfolio intelligence dashboard
  │   ├── cs5_app.py                       # Main entry point (10 pages)
  │   ├── app.py                           # Legacy entry point
  │   ├── cs4_app.py                       # CS4 chatbot interface
  │   ├── data_loader.py                   # Data loading utilities
  │   ├── requirements.txt                 # Streamlit-specific dependencies
  │   ├── components/
  │   │   ├── charts.py                    # Plotly chart components
  │   │   └── evidence_display.py          # Evidence cards + justification panels
  │   ├── views/
  │   │   ├── chatbot_cs4.py               # CS4 chatbot view
  │   │   ├── company_deep_dive.py         # Company deep dive
  │   │   ├── evidence_cs2.py              # Evidence visualization
  │   │   ├── executive_summary.py         # Executive summary
  │   │   ├── pipeline_cs4.py              # Pipeline visualization
  │   │   ├── platform_cs1.py              # Platform overview
  │   │   ├── scoring_cs3.py               # Scoring visualization
  │   │   └── testing_coverage.py          # Test coverage report
  │   └── data/
  │       └── chat_history/                # Sample chat histories
  │
  ├── dags/                                # Apache Airflow DAGs
  │   ├── evidence_indexing_dag.py         # Nightly evidence → ChromaDB (2 AM)
  │   ├── job_signals_dag.py               # Job signal collection (child)
  │   └── signal_collection_parent_dag.py  # Parent DAG orchestrator (3 AM)
  │
  ├── tests/                               # 25+ pytest test files
  │   ├── conftest.py                      # Fixtures
  │   ├── test_api.py, test_e2e.py         # API + E2E tests
  │   ├── test_redis_cache.py              # Cache tests
  │   ├── test_hybrid_retriever.py         # Retrieval tests
  │   ├── test_mcp_integration.py          # MCP tests
  │   ├── test_property_based.py           # Hypothesis property tests
  │   └── ...                              # + 15 more test modules
  │
  ├── exercises/                           # Learning materials
  │   ├── agentic_due_diligence.py         # Agentic DD example
  │   └── complete_pipeline.py             # End-to-end pipeline example
  │
  ├── data/                                # Local data cache
  │   ├── raw/, parsed/, chunks/           # SEC filing stages
  │   ├── signals/                         # Signal data cache
  │   ├── culture_cache/                   # Glassdoor culture data
  │   └── proxy_cache/                     # DEF-14A proxy cache
  │
  ├── results/                             # Pipeline output
  │   ├── {nvda,jpm,wmt,ge,dg}.json       # Per-company results
  │   ├── portfolio_summary.json           # Portfolio summary
  │   └── reports/                         # Generated IC memos + LP letters
  │
  ├── docker-compose.yml                   # API (:8000) + Redis (:6379) + Airflow (:8080)
  ├── Dockerfile                           # API container
  ├── pyproject.toml                       # Poetry config (Python >=3.11, 50+ deps)
  ├── requirements.txt                     # pip requirements
  ├── CLAUDE.md                            # Architecture guide (developer reference)
  └── API_DOCS.md                          # API endpoint documentation

